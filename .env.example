# Server-only. Do not add a VITE_ prefix. That would expose the value to the browser.
# Local: copy this file to .env.local and replace the placeholder.
# Vercel: Project → Settings → Environment Variables → GROQ_API_KEY
GROQ_API_KEY=your_key_here
